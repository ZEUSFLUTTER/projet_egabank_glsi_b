# Application
spring.datasource.url=jdbc:postgresql://localhost:5432/ega_bank
spring.datasource.username=postgres
spring.datasource.password=98875290
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


spring.main.allow-bean-definition-overriding=true

# CORS Configuration (managed via SecurityConfig)
# Configured in SecurityConfig.java using CorsConfigurationSource

# JWT Configuration
# Access Token: 3 minutes (180000 ms) - expires on inactivity
jwt.access-token.expiration=180000
# Refresh Token: 7 days (604800000 ms) - can renew session
jwt.refresh-token.expiration=604800000
jwt.secret=egaBankSecretKeyForJWTTokenGeneration2026SecureAndLongEnough

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.ega=DEBUG

